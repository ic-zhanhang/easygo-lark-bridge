---
description: 飞书桥接改动后的双机同步流程（Mac 达妮娅 → 云端 → Linux 秧秧）
alwaysApply: true
---

# 飞书桥接部署流程（必记）

本仓库 **easygo-lark-bridge** 在 Mac（达妮娅）与 Linux（秧秧）各 clone 一份；**改完必须双机同步**。

## 标准顺序（每次改桥接代码/脚本/模板后）

### 1. Mac 本地验证（可选但推荐）

```bash
cd ~/workspace/easygo-lark-bridge
# 若改了 claw 补丁或 install 脚本，Mac 侧重跑：
BRIDGE_PROFILE=mac bash scripts/install.sh
bash scripts/claw-service.sh restart
bash scripts/claw-service.sh status
```

**Agent 在本机改桥接时**：不要在任务中途 `restart`（会 SIGTERM 杀掉自己，卡片报「执行失败」）。应先跑完 `install.sh`，回复用户后用：

```bash
bash scripts/claw-service.sh restart-defer 20          # Mac
bash scripts/claw-service-linux.sh restart-defer 20    # Linux
```

`config/easygo.env`、`claw/`、`runtime/` **不入库**；各机器各自维护。

### 2. 提交并推送到云端

```bash
cd ~/workspace/easygo-lark-bridge
git add -A   # 勿 add config/easygo.env（已在 .gitignore）
git commit -m "..."
git push origin main
```

### 3. Linux 拉取并应用（秧秧）

Mac 能 SSH 时（内网 `linux-dev` 或直连 `linux-dev-direct`）：

```bash
ssh linux-dev 'export PATH="$HOME/.bun/bin:$HOME/.local/bin:$PATH"; \
  cd ~/workspace/easygo-lark-bridge && \
  git pull && \
  BRIDGE_PROFILE=linux bash scripts/install.sh && \
  bash scripts/claw-service-linux.sh restart && \
  bash scripts/claw-service-linux.sh status'
```

Linux 仓库路径与 Mac 一致：`~/workspace/easygo-lark-bridge`。

### 4. 验证

- Linux：`bash scripts/claw-service-linux.sh logs`（看 `[Bot] name=秧秧`）
- 飞书群话题里 @秧秧 发短指令

## 分工提醒

| Bot | 机器 | Profile | 路径 |
|-----|------|---------|------|
| 达妮娅 | Mac | `mac` | `~/workspace/easygo-lark-bridge` |
| 秧秧 | Linux | `linux` | `~/workspace/easygo-lark-bridge` |

- 两台 **FEISHU_APP_ID 必须不同**；open_id 也各 Bot 独立，授权/sync 按本机 `config/easygo.env` 的 `BRIDGE_PROFILE`。
- 仅改 Mac 本地 env/密钥：不必 push；仅改脚本/模板/补丁：**必须 push + Linux pull + install**。

## 离线 / 内网 Git 不可达时

用 rsync 代替 push（见 `docs/Linux安装.md` 方式 A）。

## Agent 协作

用户说「同步到 Linux / 部署秧秧 / 桥接推上去」时，**默认执行上述 2→3 全流程**，不要只改 Mac 就结束。
