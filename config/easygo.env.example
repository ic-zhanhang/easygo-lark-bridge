# 复制为 easygo.env 后填写：cp config/easygo.env.example config/easygo.env

# ── 部署 profile ────────────────────────────────────────────
# mac = 达妮娅（Mac，默认）；linux 见 config/easygo.env.linux.example
BRIDGE_PROFILE=mac

# ── 聊天权限与授权人（权限分级）────────────────────────────
# L1 AUTHORIZER：唯一可授予/撤销他人聊天权限（杨展航）；心跳摘要也固定私聊发给此人
# L2 CHAT_OPERATOR_*：可 @Bot 聊天的人（须含 AUTHORIZER）
# ⚠️ open_id 必须是【本 Bot App】视角：看 Claw 日志 [解析] sender=ou_xxx，或 L1 发「授权 @某人」由 Claw 自动写入
#    不要用 Mac lark-cli / 其它 Bot 查到的 id（同一人跨 App 的 open_id 不同）
AUTHORIZER_OPEN_ID=ou_xxxxxxxxxxxxxxxxxxxxxxxx
# L1 授权人 open_id（须与下方一致；协议群等仅放 CHAT_OPERATOR，不要放这里）
AUTHORIZER_OPEN_IDS=ou_xxxxxxxxxxxxxxxxxxxxxxxx
AUTHORIZER_NAME=杨展航
CHAT_OPERATOR_OPEN_IDS=ou_xxxxxxxxxxxxxxxxxxxxxxxx
CHAT_OPERATOR_NAMES=杨展航

# ── 路径 ────────────────────────────────────────────────────
# workspace 根（easygo / standard-fe 所在目录）
WORKSPACE_ROOT=/Users/ic/workspace

# 以下默认由 install.sh 推导，一般不必改：
#   claw/     → ${本仓库}/claw
#   runtime/  → ${本仓库}/runtime
# RUNTIME_DIR=
# CLAW_INSTALL_DIR=

EASYGO_WORKSPACE_FILE=/Users/ic/workspace/easygo-dev.code-workspace

# ── 飞书应用（与 lark-cli 共用）────────────────────────────
FEISHU_APP_ID=cli_a94621950d785bb3
FEISHU_APP_SECRET=

# ── 可选：豆包 Embedding（记忆向量检索，不填则仅 FTS 关键词搜索）──
# VOLC_EMBEDDING_API_KEY=
# VOLC_EMBEDDING_MODEL=doubao-embedding-vision-250615

# ── 「小组」旁观落盘（仅该 chat_id；未 @ 也写本机 jsonl+媒体，不起 Agent）──
# 文本：runtime/文档/小组旁观/YYYY-MM-DD.jsonl
# 媒体：runtime/文档/小组旁观/media/YYYY-MM-DD/（图片/文件/音视频；jsonl 里带 media_path）
XIAOZHU_CHAT_ID=oc_0a2bd151890eede76f4595a89e5f21c2

# 每条「小组」群消息 Tick 一次行为树；本地 Qwen 是无工具的 Social Leaf。
# 未 @：只允许自然短回复或提出候选任务，绝不自动开工。
# @Bot：通过代码权限门后才进入 Cursor Agent；两者始终共用同一个 Bot 身份。
XIAOZHU_SPEAK_GATE_ENABLED=true
XIAOZHU_OLLAMA_URL=http://127.0.0.1:11434
XIAOZHU_OLLAMA_MODEL=qwen2.5:14b
XIAOZHU_PERSONA_NAME=达妮娅
XIAOZHU_SPEAK_TIMEOUT_MS=20000
XIAOZHU_SPEAK_MIN_CONFIDENCE=0.78
XIAOZHU_SPEAK_COOLDOWN_SECONDS=90
XIAOZHU_SPEAK_CONTEXT_MESSAGES=20

# ── Cursor Agent ────────────────────────────────────────────
CURSOR_API_KEY=
CURSOR_MODEL=composer-2.5

# ── GitLab CI / MR（HEARTBEAT 可选，不填则跳过）──────────────
GITLAB_API=http://git.standard-robots.com/api/v4
GITLAB_PRIVATE_TOKEN=
GITLAB_BACKEND_PROJECT=SROS/platforms/easygo
GITLAB_FRONTEND_PROJECT=standard-fe/easygo
