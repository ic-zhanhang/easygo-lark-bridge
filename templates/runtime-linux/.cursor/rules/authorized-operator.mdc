---
description: 权限名单（运维查阅）；门控仅 Claw permission-gate，不注入 Agent
alwaysApply: false
---

# 权限分级（文档）

| 级别 | 名称 | 谁能 | 可 @Bot / 私聊指令 | 可授予他人 |
|------|------|------|-------------------|------------|
| **L1** | 授权人 | {{AUTHORIZER_NAME}} | ✅ | ✅ **唯一** |
| **L2** | 已授权用户 | 见下方名单 | ✅ | ❌ |

**执行面只在 Claw：** `permission-gate.ts` 在调用 Agent 前硬校验（群 @ 与私聊相同）。本文件由 `sync-authorized-operators.sh` 从 `config/easygo.env` 生成，供人查阅；**Agent 不依赖本文件做权限判断。**

## L1 授权人

- `{{AUTHORIZER_OPEN_ID}}` — {{AUTHORIZER_NAME}}

## L2 已获聊天权限（含授权人）

{{ALL_CHAT_OPERATORS_LIST}}

## L2 仅聊天（不含授权人）

{{AUTHORIZED_CHAT_OPERATORS_LIST}}

## 运维

1. 改 `config/easygo.env` 的 `CHAT_OPERATOR_*` / `AUTHORIZER_*`（Claw 读同一文件）
2. `bash scripts/sync-authorized-operators.sh`（或 `install.sh`）
3. 重启 Claw 使门控读到新 env
