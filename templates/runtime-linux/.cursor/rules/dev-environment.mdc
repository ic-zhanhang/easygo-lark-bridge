---
description: Linux 开发机环境（秧秧 · 按需：仿真/SSH/容器）
alwaysApply: false
---

# 开发环境（Linux）

> Agent 实测记录；网络或硬件变更后请更新。你运行在 **Linux 主机** 上，路径与命令以本机为准。

## 本机

| 项 | 值 |
|---|---|
| 主机 | `std-OptiPlex-7090`，Dell OptiPlex 7090，Ubuntu 22.04.5 |
| CPU / 内存 | i7-10700 8C16T / 15 GiB（swap 8GB） |
| 工作区 | `~/workspace/easygo`（含 `easygo-dev.code-workspace`） |
| 工作区根 | `~/workspace`（`easygo`、`standard-fe/easygo`） |
| 网卡 | `enp0s31f6`；内网 DHCP 约 `10.10.20.52/23`；网线直连 Mac 时 `192.168.2.2/24` |
| GPU | Intel 核显 + Mesa（`/dev/dri`，video+render 组） |
| 持久数据 | `~/sros` → 容器内 `/sros`（webapp、voice-assistant 等） |
| 容器 | `easygo-dev-main`，`--network=host`，工作区挂载进容器 |

不在 Linux **宿主机**装 ROS；统一进 dev 容器（ROS 2 Jazzy，镜像 `nexus3.sr/sros/easygo/dev:v0.1.19`）。

```bash
# Linux 宿主机
cd ~/workspace/easygo && ./script/run_dev_container.sh          # 进入容器
./script/run_dev_container.sh --ensure-running                    # 仅确保运行
```

容器内用户多为 `ubuntu`；workspace 路径 `/home/std/workspace/easygo`。

## 与 Mac 的关系

| 项 | 说明 |
|---|---|
| Mac | 移动开发机（M4 Max）；达妮娅 Bot 跑在 Mac |
| 本机角色 | **常开机**；编译、长任务、Gazebo 仿真主场 |
| 跨机协作 | Mac 上改完 **git push** → 本机 `git pull` 后继续；或飞书直接 @本机任务 |

## 仿真启动（容器内，zsh）

**必须用 `.zsh`，不要用 `.bash`（会报 RequiredPrefixNotSourced）。**

```bash
cd ~/workspace/easygo
source install/setup.zsh
source /opt/sros/share/easygo_perception_interfaces/local_setup.zsh
source /opt/sros/share/slam_interfaces/local_setup.zsh
export DISPLAY=:0    # 有物理显示器时
ros2 launch easygo_sim_bringup sim_easygo.launch.py
```

## 一次性/重建后必做

### ROS 接口 deb（重建容器后）

```bash
cd ~/workspace/easygo/src/easygo-app
./scripts/deploy/prepare-ros-interface-deps.sh --arch amd64
```

### voice-assistant（dev 镜像不预装）

```bash
cd ~/workspace/easygo
python3 script/deploy-dev.py localhost voice_assistant \
  -c easygo-dev-main --voice-assistant-build
```

装到 `/sros/voice-assistant/current`；数据在 `~/sros`，重启容器无需重装。

### VDA5050 子仓（若缺包）

```bash
git clone git@git.standard-robots.com:SROS/core/vda5050_ros2.git src/Vda5050_ROS2
```

## 浏览器访问（本机 / 局域网）

内网 IP 多为 `10.10.20.52`；网线直连 Mac 时为 `192.168.2.2`。

| 入口 | 本机 URL |
|---|---|
| WebApp | `http://127.0.0.1:18088` |
| Gazebo noVNC | `http://127.0.0.1:6080/vnc.html?autoconnect=true&resize=remote` |
| 观察台 | `http://127.0.0.1:8090/?left=http://127.0.0.1:18088&right=http://127.0.0.1:6080/vnc.html?autoconnect=true&resize=remote&reconnect=true` |

## 可忽略的仿真告警

- `amixer` / `nmcli` / `bluetooth` / `TTS API key`：容器无 HMI 硬件
- `Cannot open PGM: /sros/map/default_map0.pgm`：无默认地图，不影响看 Gazebo/前端
- Nav2 启动前几秒的 `lifecycle_manager ... not ready`：会自动重试成功

细节见 `easygo/README.md`、`src/easygo_sim_bringup/README.md`。
