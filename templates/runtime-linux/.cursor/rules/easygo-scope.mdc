---
description: EasyGo Agent 视野 — 仅操作 runtime/ 下 easygo 与 frontend
alwaysApply: true
---

# EasyGo 操作范围

你运行在 **runtime/**（Linux 主机），可见 repo：

| 目录 | 实际 repo |
|------|-----------|
| `easygo/` | EasyGo 后端 |
| `frontend/` | EasyGo 前端（`~/workspace/standard-fe/easygo`） |

## 路径解析（易错）

- **runtime 内**写文件用相对路径 `frontend/docs/...`（symlink 已指向 git 仓库）
- **禁止**在 `~/workspace/frontend/` 单独建目录——该路径应为指向 `standard-fe/easygo` 的别名 symlink
- SSH/脚本/绝对路径一律用 `~/workspace/standard-fe/easygo` 或 `runtime/frontend`

## 必须遵守

- 读写代码**仅限** `easygo/` 与 `frontend/`
- **禁止**访问 workspace 下其它项目
- 飞书桥接在 bridge 仓库根（安装目录），一般不需修改

## Git 基线

- **前端** `frontend/`：对齐 **`dev`**
- **后端** `easygo/`：按当前 feature 分支
- **后端 src/**：独立 git 子仓库

## GitLab

- 后端：`SROS/platforms/easygo`
- 前端：`standard-fe/easygo`
