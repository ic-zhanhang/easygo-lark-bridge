---
description: 记忆检索 — 仅文档；Topic Session 续聊；跨群硬隔离
alwaysApply: false
---

# 记忆检索（按需）

## 默认行为

- 同一群话题内可沿用 **Topic Session** 上下文；**不要**读 `topics/*.jsonl` 旁路历史。
- 不要自动 search、不要读 `.cursor/MEMORY.md`（已停用）。
- 用户**点名文件/路径** → 只 Read 该路径。
- 用户要查团队习惯等 → 可查 `文档/`（见下「硬隔离」例外）。

## 硬隔离（跨群）

除非当前任务明确来自「小组」会话（prompt 里由桥接标明小组/水位上下文），否则：

- **禁止**读 `文档/小组旁观/`、`文档/小组日报/`、`state/xiaozu-groups/`
- **禁止**把其它群的备忘（如「来源：飞书小组」的 `文档/*.md`）当成**当前群**对话上下文
- 上下文不足时：以 prompt 里桥接注入的 **[当前飞书群话题消息]** 为准；不要用 memory-tool 去「找个最近的需求」顶替

## 可选：memory-tool（仅用户要查团队文档时）

```bash
# 在 runtime/ 工作区内执行
bun "${BRIDGE_ROOT:-../..}/claw/memory-tool.ts" stats
bun "${BRIDGE_ROOT:-../..}/claw/memory-tool.ts" search "关键词"
```

索引范围由桥接限定为 `文档/`（已排除小组旁观/日报）；不以 topics jsonl 为对话上下文源。
