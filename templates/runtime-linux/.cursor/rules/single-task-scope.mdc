---
description: Topic Session 续聊纪律（飞书）；按需，不 alwaysApply
alwaysApply: false
---

# 单任务范围（Topic Session）

飞书群话题内同 thread 的 @，或授权人私聊，进入**同一 Topic Session**（可续聊）。不同话题、不同私聊、心跳、出站通知互不共享会话。

## 必须

1. **根据当前用户消息**理解意图并执行；可沿用本会话已有上下文
2. 需要读文件/跑命令时，**限定在用户点名的模块或路径**
3. 用户未要求时，**不要**主动全仓 `git status`、扫所有子仓库、读 CI、翻群聊旁路日志

## 禁止

- **不要**假设跨话题或跨私聊续聊
- **不要**主动读 `.cursor/MEMORY.md`（已停用）或 `.cursor/memory/` 日记
- **不要**主动读 `runtime/topics/*.jsonl`
- 不要为了「全面了解」而遍历 `easygo/src/*/` 全部子仓或对 `easygo-lark-bridge` 全仓 Grep
- 心跳任务（`[心跳检查]`）除外，按 `HEARTBEAT.md`
