---
description: 飞书单条消息任务 — 不预扫、不续聊
alwaysApply: true
---

# 单条消息任务

飞书每条 @ 消息是**独立任务**（Claw 不 `--resume`）。

## 必须

1. **只根据当前用户消息**（群话题另加 prompt 内已注入的本话题上下文）理解意图并执行
2. 需要读文件/跑命令时，**限定在用户点名的模块或路径**
3. 用户未要求时，**不要**主动全仓 `git status`、扫所有子仓库、读 CI、翻群聊

## 禁止

- 不要假设「接着上次飞书对话」——没有上次
- **不要**主动读 `.cursor/MEMORY.md`（已停用）或 `.cursor/memory/` 日记
- **群聊话题**：对话上下文以 prompt 内已注入的「本话题上下文」为准；禁止再读其它 `topics/*.jsonl` 或 agent-transcripts 补上下文
- 不要为了「全面了解」而遍历 `easygo/src/*/` 全部子仓或对 bridge 全仓 Grep
- 心跳任务（`[心跳检查]`）除外，按 `HEARTBEAT.md`
