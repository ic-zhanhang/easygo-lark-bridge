---
description: 话题模式 — 本机存储、默认读群话题上下文、Git 与 Linux 仿真
alwaysApply: false
---

# 话题模式

## 我怎么处理消息（第一人称，与用户说明一致）

- **群聊话题**：每次 @我，桥接会默认附上本话题本地记录路径；我会先读该文件再执行（含此前 @ 与 Bot 回复、图片路径）。
- **私聊**：默认仍只看当前这一条；若你要结合过往，请明确说（如「读一下这个话题里的所有消息」）。
- 话题记录存在本机 `runtime/topics/`（约 3 天）。
- 你发的**图片**我会下载到本机，路径写入话题记录；当前任务 prompt 里也会带 `[附件图片: 路径]`。
- 答复过程中飞书卡片会显示 **思考中 / 执行工具 / 回复中** 中间态。

## Git

- 不是每个话题都建分支；**只有判断需要改仓库代码**时，才为一个话题绑定**一个**分支，并在回复中说明分支名。
- 同一话题后续改代码，继续用已绑定分支。

## Linux 仿真

- **整台机器同时只能跑 1 个仿真**（看本机是否已有 Gazebo / sim launch 进程）。
- 若已有仿真在跑，我会直接说明，不会另起第二个。
