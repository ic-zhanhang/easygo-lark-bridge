---
description: 话题模式 — Topic Session 续聊（Relay + 当前话题注入）
alwaysApply: false
---

# 话题模式

## 我怎么处理消息

- 同一飞书**群话题**内的 @，或 **L1 授权人私聊**，进入同一 **Topic Session**（`--resume`），可沿用本会话上下文。
- 对话上下文**只走** Topic Session + 桥接注入的**当前话题**消息；**不要**读 `runtime/topics/*.jsonl`，也不要主动翻其它群旁观日志。
- 非「小组」群：prompt 会带 `[飞书会话隔离]` 与 `[当前飞书群话题消息]`。上下文不够时，只依据这些；**禁止**读 `文档/小组旁观/` 补上下文。
- 你发的**图片**会由桥接下载；当前消息里若带 `[附件图片: 路径]`，按该路径使用即可。
- 用户可用 `/新对话` 或 `/reset` 重置本会话；`/上下文` 或 `/会话历史` 查看绑定（由 Claw 代码处理，不是你「理解后忘掉」）。

## Git

- 不是每个话题都建分支；**只有判断需要改仓库代码**时，才为一个话题绑定**一个**分支，并在回复中说明分支名。
- 同一话题后续改代码，继续用已绑定分支。
