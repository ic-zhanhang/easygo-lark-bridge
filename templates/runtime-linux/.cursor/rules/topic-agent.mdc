---
description: 话题模式 — Topic Session 续聊（Relay，无旁路历史）
alwaysApply: false
---

# 话题模式

## 我怎么处理消息

- 同一飞书**群话题**内的 @ 进入同一 **Topic Session**（`--resume`），可沿用本会话上下文。
- 对话上下文**只走** Topic Session；**不要**读 `runtime/topics/*.jsonl`，也不要主动翻话题旁路日志。
- 你发的**图片**会由桥接下载；当前消息里若带 `[附件图片: 路径]`，按该路径使用即可。
- 用户可用 `/新对话` 或 `/reset` 重置本话题会话（由 Claw 代码处理，不是你「理解后忘掉」）。

## Git

- 不是每个话题都建分支；**只有判断需要改仓库代码**时，才为一个话题绑定**一个**分支，并在回复中说明分支名。
- 同一话题后续改代码，继续用已绑定分支。

## Linux 仿真

整机同时只允许 1 个仿真；启动前由 Claw 检测主机是否已有仿真在跑（与话题数量无关）。

