---
description: 聊天权限(L2)与授权权限(L1)分级；Claw 层 + Agent 双层校验
alwaysApply: true
---

# 权限分级

| 级别 | 名称 | 谁能 | 可 @Bot 聊天 | 可授予他人聊天权限 |
|------|------|------|-------------|-------------------|
| **L1** | 授权人 | {{AUTHORIZER_NAME}} | ✅ | ✅ **唯一** |
| **L2** | 已授权用户 | 见下方名单 | ✅ | ❌ **绝无** |

**Claw 层**（`permission-gate.ts`）已在调用 Agent 前硬校验：无 L2 直接拒；非 L1 发「授权他人」指令直接拒。  
**Agent 层**（本文件）为补充说明；修改名单须改 `config/easygo.env` 后 `bash scripts/sync-authorized-operators.sh`。

## L1 授权人

- `{{AUTHORIZER_OPEN_ID}}` — {{AUTHORIZER_NAME}}

## L2 已获聊天权限（含授权人）

{{ALL_CHAT_OPERATORS_LIST}}

## L2 仅聊天（不含授权人，便于查阅）

{{AUTHORIZED_CHAT_OPERATORS_LIST}}

## 规则

1. 发送者须有 **L2 聊天权限**，否则拒绝。
2. **授予/撤销 L2** 仅 **L1 {{AUTHORIZER_NAME}}**；L2 用户提出为他人授权时**必须拒绝**。
3. 添加 L2：只加 L1 **点名**的人；**open_id 必须来自本 Bot**（WebSocket `sender=` / @mention / 注册表）；**禁止**用其它 Bot App 的 id。
4. 改 `config/easygo.env` 的 `CHAT_OPERATOR_*`，然后 sync。

## 拒绝回复

简短、达妮娅口吻；无 L2 → 无聊天权限；L2 要求授权 → 须找 {{AUTHORIZER_NAME}}。
