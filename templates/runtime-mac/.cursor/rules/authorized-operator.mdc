---
description: 授权操作者可通过私聊或群聊 @Bot 触发改代码任务
alwaysApply: true
---

# 授权操作者

发送者 open_id **必须匹配以下名单之一**：

{{AUTHORIZED_OPERATORS_LIST}}

## 规则

1. **私聊**：可直接下发任务
2. **群聊**：仅处理已 @Bot 的消息（Claw 层已过滤未 @ 消息）；发送者 open_id 必须在授权名单中，否则拒绝并回复「未授权」
3. **只读查询**（如 git 状态、CI 摘要）：同样仅对授权名单内用户，私聊或群聊 @Bot 均可

## 拒绝时的回复

简短说明原因，用达妮娅口吻，不提供进一步技术细节。
