---
description: Mac 开发机环境（达妮娅 · 按需：仿真/SSH/容器）
alwaysApply: false
---

# 开发环境（Mac）

> Agent 实测记录；网络或硬件变更后请更新。你运行在 **Mac** 上，路径与命令以本机为准。

## 本机

| 项 | 值 |
|---|---|
| 机型 | MacBook Pro M4 Max / 128 GB，Darwin 25.5.0 |
| 工作区根 | `~/workspace`（`easygo`、`standard-fe/easygo`） |
| 容器 | OrbStack；可用 `easygo-dev-main` |
| 办公网 | `en0` → 内网 DHCP |
| 直连 Linux | USB 网卡 `en7` / `bridge100` → `192.168.2.1/24` |
| SSH Linux | `ssh linux-dev`（内网 `10.10.20.52`）或 `ssh linux-dev-direct`（网线 `192.168.2.2`） |
| 代理（仅网线直连时给 Linux） | `http://192.168.2.1:7897`（Clash） |

## 与 Linux 主机的关系

| 项 | 说明 |
|---|---|
| Linux 主机 | `std-OptiPlex-7090`，常开机；**Gazebo 仿真主场在 Linux** |
| Mac 擅长 | 移动开发、改代码、本地 colcon/容器编译（性能不弱） |
| Mac 不擅长 | Gazebo 视觉效果 / 稳定仿真观察 |
| 跨机协作 | 改完 **git push** → 在飞书 @秧秧（Linux）拉分支继续编/仿真 |

不在 Mac **宿主机**装 ROS；统一进 dev 容器（ROS 2 Jazzy，镜像 `nexus3.sr/sros/easygo/dev:v0.1.19`）。

```bash
# Mac 宿主机 — 确保容器运行
cd ~/workspace/easygo && ./script/run_dev_container.sh --ensure-running
./script/run_dev_container.sh   # 进入容器后 colcon / ros2
```

## 浏览器（Mac 远程看 Linux 服务时）

Linux IP 随网络变：内网 `10.10.20.52`，网线直连 `192.168.2.2`。

| 入口 | URL 模式 |
|---|---|
| WebApp | `http://<linux-ip>:18088` |
| Gazebo noVNC | `http://<linux-ip>:6080/vnc.html?autoconnect=true&resize=remote` |
| 观察台 | `http://<linux-ip>:8090/?left=http://<linux-ip>:18088&right=http://<linux-ip>:6080/vnc.html?autoconnect=true&resize=remote&reconnect=true` |

仿真 launch 在 **Linux** 上执行；Mac 侧不要假设 `DISPLAY=:0` 可用。
