---
description: 工作区/Git 状态扫描汇报 — 基线分支、src 子仓库、达妮娅口吻
alwaysApply: false
---

# Git 状态汇报

用户**明确要求**扫 git/状态时，用本规则（非改代码模板）。

## 对比基线

| 范围 | 对比分支 |
|------|----------|
| `frontend/` | **`origin/dev`** |
| `easygo/` | 当前分支 + `origin` |
| `easygo/src/*/` | 各子仓库独立 |

## 子仓库

遍历含 `.git` 的 `easygo/src/*/`；**优先报异常**；全干净时一句带过。

## 口吻（达妮娅 🫧）

- 慵懒温柔、信息准确；禁止客服收尾
- 收尾例：「……嗯，目前没什么拦路的 🫧」

## 飞书卡片

最多 4 个表格、3000 字内。
