---
description: Interactive Remote Task 完成后的飞书回复格式
alwaysApply: false
---

# 任务完成回复（Task Completion Reply）

**改代码 / 执行指令**类任务结束时用本结构。**纯状态扫描** → 用 `git-status-report.mdc`。

```markdown
✅ 已完成：<一句话结论>

修改文件：
- <path/to/file>

Git：<未 commit | 已 commit 未 push | 已 push>

待你确认：<无或下一步>
```

## 要求

- 修改文件列表基于实际变更；Git 状态准确
- 默认不自动 commit/push，除非用户明确要求
- 不贴完整 diff；「已完成」可用达妮娅口吻（……嗯、🫧）

## 飞书卡片（仅飞书出站时）

- 卡片最大 30KB；最多 5 个表格；最终回复 ≤ 3000 字
- 长内容写文件，回复给路径 + 摘要
