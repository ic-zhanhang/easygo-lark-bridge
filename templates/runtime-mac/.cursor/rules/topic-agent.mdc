---
description: 话题模式 — 本机存储、群话题默认注入本话题上下文、Git 与 Linux 仿真
alwaysApply: false
---

# 话题模式

## 我怎么处理消息（第一人称，与用户说明一致）

- **群聊话题**：每次 @我，桥接会把**本话题**历史直接注入当前任务 prompt（用户 @、Bot 回复、图片路径）。我只依据这份上下文 + 当前消息处理。
- **私聊**：默认仍只看当前这一条；若你要结合过往，请明确说（如「读一下这个话题里的所有消息」）——那时才会注入对应私聊记录。
- 话题记录存在本机 `runtime/topics/`（约 3 天）；归档文件路径会出现在 prompt 里，**无需再 Read**，除非你要我核对原文。
- **隔离**：不读其它话题的 jsonl，不用 MEMORY / agent-transcripts / 其它群聊去补「对话上下文」。
- 你发的**图片**我会下载到本机，路径写入话题记录；当前任务 prompt 里也会带 `[附件图片: 路径]`。
- 答复过程中飞书卡片会显示 **思考中 / 执行工具 / 回复中** 中间态。

## Git

- 不是每个话题都建分支；**只有判断需要改仓库代码**时，才为一个话题绑定**一个**分支，并在回复中说明分支名。
- 同一话题后续改代码，继续用已绑定分支。

## Linux 仿真

- **整台机器同时只能跑 1 个仿真**（看本机是否已有 Gazebo / sim launch 进程）。
- 若已有仿真在跑，我会直接说明，不会另起第二个。
