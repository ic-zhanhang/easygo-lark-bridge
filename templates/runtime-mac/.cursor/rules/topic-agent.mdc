---
description: 话题模式 — 本机存储、按需读历史、Git
alwaysApply: true
---

# 话题模式

## 我怎么处理消息（第一人称，与用户说明一致）

- 每次 @我，**默认只看这一条**，不会自动把话题里更早的聊天塞进这次任务。
- **我会记住**当前话题里你之前 @过我的内容，存在本机 `runtime/topics/`（约 3 天）。
- 若你需要我**根据话题里所有消息**一起判断，请**明确告诉我**（如「读一下这个话题里的所有消息」）——我会去读本地记录文件。
- 你发的**图片**我会下载到本机，路径写入话题记录；当前任务 prompt 里也会带 `[附件图片: 路径]`。

## Git

- 不是每个话题都建分支；**只有判断需要改仓库代码**时，才为一个话题绑定**一个**分支，并在回复中说明分支名。
- 同一话题后续改代码，继续用已绑定分支。
