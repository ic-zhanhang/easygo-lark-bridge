---
description: 聊天权限与授权规则；仅授权人可授予他人聊天权限
alwaysApply: true
---

# 聊天权限与授权

## 两类身份（必须区分）

| 身份 | 人员 | 可 @Bot 聊天/执行任务 | 可授予他人聊天权限 |
|------|------|----------------------|-------------------|
| **授权人** | {{AUTHORIZER_LABEL}} | ✅ | ✅ **仅此人** |
| **已授权用户** | 见下方名单（若有） | ✅ | ❌ **绝无** |

**授权人**与**已授权用户**的区别：后者只有聊天权限，**没有授权权限**，不能替他人开通、不能修改名单。

## 授权人

- `{{AUTHORIZER_OPEN_ID}}` — {{AUTHORIZER_NAME}}

## 已获聊天权限的用户

{{AUTHORIZED_CHAT_OPERATORS_LIST}}

（若上表写「（暂无）」表示除授权人外尚无其他聊天用户。）

## 执行任务前的检查

1. 发送者 open_id 必须是**授权人**，或在**已获聊天权限**名单中；否则拒绝，简短说明未获聊天权限。
2. **私聊**：授权人 / 已授权用户均可直接下发任务。
3. **群聊**：须已 @Bot（Claw 层已过滤）；发送者仍须通过上条权限检查。
4. **只读查询**：同样须具备聊天权限。

## 授予 / 撤销聊天权限（仅授权人）

- **只有授权人 {{AUTHORIZER_NAME}}** 可以指令「给某某开聊天权限」「移除某某权限」等。
- 已授权用户若提出为他人授权：**必须拒绝**，说明只有 {{AUTHORIZER_NAME}} 可以授权，自己只有聊天权限、没有授权权限。
- 授权人确认添加时：将其 open_id 写入 `config/easygo.env` 的 `ALLOWED_OPERATOR_OPEN_IDS`（及 `ALLOWED_OPERATOR_NAMES`），执行 `bash scripts/sync-authorized-operators.sh`，**不得**把新用户设为授权人。
- 撤销时从上述名单移除并重新 sync；**授权人本人不可被移除**。

## 拒绝时的回复

简短说明；未授权只说没有聊天权限；非授权人要求授权则说明须找 {{AUTHORIZER_NAME}}。
