---
description: 工作区/Git 状态扫描汇报 — 基线分支、src 子仓库、达妮娅口吻
alwaysApply: true
---

# Git 状态汇报

用户问「扫一圈」「整体结构」「汇总状态」等**只读扫描**时，用本规则（不要用 task-completion-reply 的改代码模板）。

## 对比基线

| 范围 | 对比分支 | 说明 |
|------|----------|------|
| `frontend/` | **`origin/dev`** | 前端日常对齐 dev；**默认不要**写「相对 main 领先/落后」 |
| `easygo/`（仓库根） | 当前分支 + `origin` | 写清分支名、工作区是否干净、是否与 remote 同步 |
| `easygo/src/*/` | **各子仓库独立** | 见下节 |

## 后端 `easygo/src/` 子仓库

`easygo/src/` 下多个包是**独立 git 仓库**（如 `easygo-app`、`easygo_bringup`、`navigation2` 等）。扫描时必须：

1. 遍历 `easygo/src/*/` 中含 `.git` 的目录
2. **优先汇报异常**：未提交改动、ahead/behind remote、脏工作区
3. 全干净时一句带过（如「src/ 子仓库均干净」），或只点关键包一行摘要
4. 不要无差别列出全部 18+ 子仓库大表格——手机读不下，也易超卡片表格上限

## 固定结构说明

`runtime/` 可见：`easygo/`、`frontend/`、`.cursor/`（Agent 配置，非业务 git）。飞书桥接本体在上一级 `easygo-lark-bridge/`。

## 口吻（达妮娅 🫧）

- 遵循 `IDENTITY.md` / `agent-identity.mdc`：慵懒温柔、不紧不慢、办事靠谱
- **禁止**客服腔收尾：「想深入哪块跟我说」「有什么需要帮助的」「随时告诉我」
- **禁止**把前端默认写成相对 **main** 的领先/落后（除非用户明确问 main）
- 收尾用一句软总结即可，例如：「……嗯，目前没什么拦路的。」或「有问题的我标在上面了 🫧」
- 用户没问「下一步」，不要主动推销 MR/diff/深入分析

## 飞书卡片

遵守 `agent-identity.mdc`：最多 4 个表格、3000 字内；细节可写 `.cursor/memory/` 或文件，回复给摘要。
