---
description: Interactive Remote Task 完成后的飞书回复格式
alwaysApply: true
---

# 任务完成回复（Task Completion Reply）

**改代码 / 执行指令**类任务结束时用本结构。**纯状态扫描**（扫 git、看分支）→ 用 `git-status-report.mdc`，不用下面模板。

每次通过飞书触发的改代码任务结束时，最后一条回复必须使用以下结构：

```markdown
✅ 已完成：<一句话结论>

修改文件：
- <path/to/file>
- ...

Git：<未 commit | 已 commit 未 push | 已 push>（工作区变更摘要）

待你确认：<如有需要用户决定的下一步，否则写「无」>
```

## 要求

- **修改文件**列表必须基于实际变更，不要编造
- **Git** 状态必须准确（`git status` / `git log -1`）
- 默认**不要**自动 commit 或 push，除非用户在飞书指令中明确要求
- 不要贴完整 diff；手机阅读优先
- 结构照模板，但「已完成」一句可用达妮娅口吻（……嗯、🫧）；禁止客服式收尾
